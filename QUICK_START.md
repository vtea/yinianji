# 快速开始指南

## 🚀 本地运行（开发测试）

### 步骤 1：安装依赖
```bash
npm install
```

### 步骤 2：构建 CSS
```bash
npm run build-css
```

### 步骤 3：启动服务器
```bash
npm start
```

### 步骤 4：访问应用
打开浏览器：`http://localhost:3000`

---

## 🐳 Docker 运行（生产环境）

### 一键启动
```bash
docker-compose up --build
```

**Docker 会自动执行：**
1. ✅ 安装所有依赖（包括 Tailwind CSS 构建工具）
2. ✅ 自动构建 CSS（`npm run build-css`）
3. ✅ 启动服务器

**无需手动构建 CSS！**

### 后台运行
```bash
docker-compose up -d --build
```

### 查看日志
```bash
docker-compose logs -f
```

### 停止服务
```bash
docker-compose down
```

---

## 📝 开发模式（推荐）

如果你在开发时频繁修改样式，建议使用两个终端：

**终端 1：CSS 监听模式**
```bash
npm run dev-css
```
这会自动监听文件变化并重新构建 CSS。

**终端 2：启动服务器**
```bash
npm start
```

---

## ⚠️ 重要提示

1. **首次运行前必须构建 CSS**：`npm run build-css`
2. **Docker 构建会自动执行 CSS 构建**，无需手动操作
3. **修改样式后**：如果使用 `dev-css` 会自动重建，否则需要手动运行 `npm run build-css`
4. **`public/output.css` 已添加到 .gitignore**，不会提交到 Git

---

## 🔧 故障排除

### CSS 样式不生效？
- 检查 `public/output.css` 是否存在
- 清除浏览器缓存（Ctrl+Shift+R）
- 检查浏览器控制台是否有错误

### Docker 构建失败？
- 检查网络连接
- 查看构建日志：`docker-compose build --no-cache`
